#include <stdio.h>
#include <stdlib.h>

int main() {
  int a, b, c;
  scanf("%d", &a);
  scanf("%d", &b);
  scanf("%d", &c);
  
  int maior = a; 
  if (b > maior) maior = b; 
  if (c > maior) maior = c; 
  
  printf("%d eh o maior\n", maior);
  
  return 0;
}
